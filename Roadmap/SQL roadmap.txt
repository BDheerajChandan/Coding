SQL 
====
1. show,use,create databases and tables
2. CURD :
3. DQL : SELECT + (WHERE, NOT ,AS, DISTINCT, IN, IS, NULL)
4. DDL : CREATE, ALTER, DROP, TRUNCATE, RENAME
5. DML : INSERT, UPDATE(SET, WHERE), DELETE
6. LIKE(% _), REGEXP(^ $ | [] -) 
7. COUNT, ORDER BY, GROUP BY, ASC, DEC, LIMIT, OFFSET
8. UNION + JOIN(LEFT , RIGHT , OUTER , INNER , CROSS, NATURAL, ON, USING)
9. views and stored procedures

.   DCL : GRANT, REVOKE
.   TCL : COMIT, ROLLBACK, SAVEPOINT, RELEASE SAVEPOINT, SET TRANSACTION
  
